# ══════════════════════════════════════════════════════════════
#  requirements.txt
#  Proyek: Klasifikasi GitHub Issues .NET — ML Pipeline
#  Python  >= 3.9
# ══════════════════════════════════════════════════════════════

# ── Scraping ───────────────────────────────────────────────────
requests>=2.31.0          # HTTP requests ke GitHub REST API
tqdm>=4.65.0              # Progress bar scraping

# ── Data & Numerik ─────────────────────────────────────────────
pandas>=2.0.0             # Manipulasi DataFrame, baca/tulis CSV
numpy>=1.24.0             # Operasi array dan log-transform

# ── Machine Learning ───────────────────────────────────────────
scikit-learn>=1.3.0       # TF-IDF, LR, SVM, metrics, train_test_split
scipy>=1.11.0             # Sparse matrix (hstack, csr_matrix)

# ── Visualisasi ────────────────────────────────────────────────
matplotlib>=3.7.0         # Plot confusion matrix, bar chart
seaborn>=0.12.0           # Heatmap confusion matrix

# ── File Output ────────────────────────────────────────────────
openpyxl>=3.1.0           # Ekspor Excel (opsional)

# ── Notebook ───────────────────────────────────────────────────
ipykernel>=6.25.0
jupyter>=1.0.0
